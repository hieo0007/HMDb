{
  "version": 2,
  "buildCommand": "cd cinerate-landing && npm install && npm run build",
  "outputDirectory": "cinerate-landing/dist",
  "installCommand": "npm install && cd cinerate-landing && npm install",
  "env": {
    "NODE_ENV": "production"
  },
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
